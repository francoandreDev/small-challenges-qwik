import { component$ } from "@builder.io/qwik";

export const App = component$(() => {
    return <div class="text-neutral-400 bg-gray-900 h-full min-h-screen flex flex-col flex-wrap gap-2 items-center py-6 font-bold text-5xl">
        <h1>My App</h1>
    </div>
})